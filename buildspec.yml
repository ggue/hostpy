version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.7
  pre_build:
    commands:
      - pip3 install django
artifacts:
  files:
    - '**/*'
  base-directory: hostname-web-$(date +%Y-%m-%d)
  
